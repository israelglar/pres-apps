# Supabase Configuration
# Get these values from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api

# Public Supabase URL (safe to expose to client)
VITE_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Public Anonymous Key (safe to expose to client, protected by RLS policies)
VITE_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key (NEVER expose to client, only use in server-side scripts)
# Only needed for database management scripts (npm run db:*)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Setup Instructions:
# 1. Copy this file to .env in the project root
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. Never commit the .env file to version control (it's in .gitignore)
# 4. For production deployment (Vercel), set these as environment variables in the dashboard
